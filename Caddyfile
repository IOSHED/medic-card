test-med.ru {
    reverse_proxy http://django:8000 {
        header_up X-Forwarded-Proto {scheme}
        header_up X-Forwarded-Host {host}
        header_up Host {host}

        # Без этих заголовков могут быть проблемы с health checks
        header_down -Server
    }
    file_server /static/*
}
